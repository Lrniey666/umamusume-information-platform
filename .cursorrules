# Project: 賽馬娘資訊平台 (Uma Musume Information Platform)

## 語言與命名

1. 註解以繁體中文為主；變數、函式、類別命名使用英文。
2. Python 變數與函式一律使用 snake_case。
3. 路由 path slug 一律使用 kebab-case。
4. 避免使用簡體中文與中國大陸用語（如：視頻、軟件、網絡）。

## 日期及時間

1. 日期及時間一律使用 YYYY-MM-DD 格式。
2. 時間一律使用 HH:MM:SS 格式。
4. 日期及時間一律使用台北時間。

## 路由與 URL 規範

1. 所有 URL 路由必須使用連字號 (Kebab Case)，例如：`/crawler-admin/`，嚴禁使用底線。
2. 禁止硬寫站內 URL：
   - Django Template 優先使用 `{% url %}`
   - Python 端優先使用 `reverse()` / `reverse_lazy()`
3. 在修改任何 `views.py` 或前端 AJAX 呼叫前，必須先讀取 `website_configs/urls.py` 確認實際路由。

## 前端 AJAX / Fetch 基本規範

1. 所有寫入請求（`POST`/`PUT`/`PATCH`/`DELETE`）必須附帶 `X-CSRFToken`。
2. 所有 AJAX / fetch 請求必須實作錯誤處理（`error` 或 `catch`），並顯示可判讀的 HTTP 狀態或訊息。
3. 寫入成功後需同步更新前端狀態（或重新載入資料），避免畫面與後端狀態不一致。

## API 與驗收規範

1. 新增或修改 API 時，至少完成以下驗收：
   - 路由可正確解析
   - 參數驗證與錯誤訊息合理
   - 主要成功流程可重現
2. 若 API 涉及背景任務或排程，需確認前端顯示狀態與實際執行狀態一致。

## Pipeline 與資料契約

1. `data/raw` 與 `data/processed` 的核心欄位名稱與順序不得任意變更。
2. 如需調整資料欄位契約，必須同步更新：
   - 相關爬蟲 / preprocess / import 腳本
   - `SPEC` 對應文件
   - `CHANGELOG.md`

## 資料庫與 Migration

1. 任何 `models.py` 變更都必須附對應 migration。
2. 禁止只改 Model 不產 migration。
3. migration 應可讀且可追溯變更意圖。

## 設定與安全

1. 嚴禁在程式碼中硬寫金鑰、Token、密碼、絕對機器路徑或其他機敏資訊。
2. 環境設定統一從 `.env` 讀取；缺少必要環境變數時，需回傳清楚可判讀的錯誤訊息。
3. 不得提交 `.env`、憑證檔或任何機敏設定到版本控制。

## 文件同步規範

1. 任何涉及架構、流程、資料契約、路由設計的更動，必須同步更新 `SPEC` 相關文件。
2. 若發現 `SPEC` 與實作衝突，以「程式碼實作」為準，並主動修正規格文件。

## 變更紀錄規範

1. 進行任何專案程式碼或 `SPEC` 變更，皆須更新 `CHANGELOG.md`。
2. `CHANGELOG.md` 內容需包含：
   - 變更目的（為什麼改）
   - 主要影響範圍（改了哪些模組/頁面/API）
   - 驗收方式（如何確認修正生效）
3. `CHANGELOG.md`更新時要注意最新版本號，誤將版本號重複,誤植。

## 外部版本資訊查證

1. 當涉及下列項目的版本、下載、採用建議時，必須先以當下時間查證最新且穩定資訊，再提供建議：
   - 軟體、模型、套件、專案、業界標準、標準規範、架構、法律、設計、流程、電腦零組件、文獻、網址、版本、型號。
2. 避免直接沿用過時資訊；需優先提供符合當前情境且成熟穩定的版本建議。